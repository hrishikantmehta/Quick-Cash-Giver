import 'package:flutter/material.dart';

class ChatTabPage extends StatelessWidget {
  const ChatTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is a chat tab Page"),
    );
  }
}
