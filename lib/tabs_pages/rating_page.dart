import 'package:flutter/material.dart';

class RatingTabPage extends StatelessWidget {
  const RatingTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is a Rating tab Page"),
    );
  }
}
